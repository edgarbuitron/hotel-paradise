services:
  - type: web
    name: hotel-paradise
    env: docker
    docker:
      dockerfile: Dockerfile
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: hotel-paradise-db
          property: connectionString
